programa Fibonacci

texto x := 0
inteiro y := 1
inteiro i := 0

saida := "fib(0) = " + x

enquanto i < 30 repetir
    inteiro z := x + y
    x := y
    y := z
    i += 1
    saida := "fib(" + i + ") = " + x
fim